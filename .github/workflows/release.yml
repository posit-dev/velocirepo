name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  validate-tag:
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.validate.outputs.tag_name }}
      version: ${{ steps.validate.outputs.version }}
    steps:
      - name: Validate release tag
        id: validate
        run: |
          TAG_NAME="${GITHUB_REF#refs/tags/}"
          if [[ ! "$TAG_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid release tag: $TAG_NAME"
            echo "Expected format: vMAJOR.MINOR.PATCH"
            exit 1
          fi
          {
            echo "tag_name=$TAG_NAME"
            echo "version=${TAG_NAME#v}"
          } >> "$GITHUB_OUTPUT"

  build-linux:
    needs: validate-tag
    strategy:
      fail-fast: false
      matrix:
        include:
          - goarch: amd64
            runner: ubuntu-latest
          - goarch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build
        env:
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '1'
        run: |
          VERSION="${{ needs.validate-tag.outputs.version }}"
          mkdir -p dist
          LDFLAGS="-s -w -X github.com/posit-dev/velocirepo/internal/version.Version=v${VERSION} -X github.com/posit-dev/velocirepo/internal/version.Commit=$(printf '%s' "$GITHUB_SHA" | cut -c1-8) -X github.com/posit-dev/velocirepo/internal/version.Date=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          go build -trimpath -ldflags="$LDFLAGS" -o dist/velocirepo ./cmd/velocirepo

          echo "--- Binary info ---"
          file dist/velocirepo

          # Smoke test
          echo "--- Smoke test ---"
          SMOKE_OUT=$(dist/velocirepo version 2>&1)
          echo "$SMOKE_OUT"

          cp dist/velocirepo "dist/velocirepo-linux-${{ matrix.goarch }}"
          cd dist
          tar czf "velocirepo_${VERSION}_linux_${{ matrix.goarch }}.tar.gz" velocirepo
          rm velocirepo

      - uses: actions/upload-artifact@v5
        with:
          name: velocirepo-linux-${{ matrix.goarch }}
          path: |
            dist/*.tar.gz
            dist/velocirepo-linux-${{ matrix.goarch }}

  build-darwin:
    needs: validate-tag
    strategy:
      matrix:
        include:
          - goarch: amd64
            runner: macos-latest
          - goarch: arm64
            runner: macos-latest
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build
        env:
          GOOS: darwin
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '1'
        run: |
          VERSION="${{ needs.validate-tag.outputs.version }}"
          mkdir -p dist
          LDFLAGS="-s -w -X github.com/posit-dev/velocirepo/internal/version.Version=v${VERSION} -X github.com/posit-dev/velocirepo/internal/version.Commit=$(printf '%s' "$GITHUB_SHA" | cut -c1-8) -X github.com/posit-dev/velocirepo/internal/version.Date=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          go build -trimpath -ldflags="$LDFLAGS" -o dist/velocirepo ./cmd/velocirepo

          echo "--- Binary info ---"
          file dist/velocirepo

          # Smoke test (only for native arch)
          NATIVE_ARCH=$(uname -m)
          if [ "${{ matrix.goarch }}" = "arm64" ] && [ "$NATIVE_ARCH" = "arm64" ]; then
            echo "--- Smoke test ---"
            SMOKE_OUT=$(dist/velocirepo version 2>&1)
            echo "$SMOKE_OUT"
          fi

          cp dist/velocirepo "dist/velocirepo-darwin-${{ matrix.goarch }}"
          cd dist
          tar czf "velocirepo_${VERSION}_darwin_${{ matrix.goarch }}.tar.gz" velocirepo
          rm velocirepo

      - uses: actions/upload-artifact@v5
        with:
          name: velocirepo-darwin-${{ matrix.goarch }}
          path: |
            dist/*.tar.gz
            dist/velocirepo-darwin-${{ matrix.goarch }}

  build-windows:
    if: false  # temporarily disabled
    needs: validate-tag
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: '1'
        run: |
          VERSION="${{ needs.validate-tag.outputs.version }}"
          mkdir -p dist
          LDFLAGS="-s -w -X github.com/posit-dev/velocirepo/internal/version.Version=v${VERSION} -X github.com/posit-dev/velocirepo/internal/version.Commit=$(printf '%s' "$GITHUB_SHA" | cut -c1-8) -X github.com/posit-dev/velocirepo/internal/version.Date=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          go build -trimpath -ldflags="$LDFLAGS" -o dist/velocirepo.exe ./cmd/velocirepo

          # Smoke test
          echo "--- Smoke test ---"
          dist/velocirepo.exe version

          cp dist/velocirepo.exe "dist/velocirepo-windows-amd64.exe"
          cd dist
          powershell -Command "Compress-Archive -Path velocirepo.exe -DestinationPath 'velocirepo_${VERSION}_windows_amd64.zip'"
          rm velocirepo.exe

      - uses: actions/upload-artifact@v5
        with:
          name: velocirepo-windows-amd64
          path: |
            dist/*.zip
            dist/velocirepo-windows-amd64.exe

  release:
    needs: [validate-tag, build-linux, build-darwin]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum *.tar.gz velocirepo-* > SHA256SUMS
          cat SHA256SUMS

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/*.tar.gz
            artifacts/SHA256SUMS
          generate_release_notes: true

      - name: Update floating version tags
        run: |
          TAG="${{ needs.validate-tag.outputs.tag_name }}"
          MAJOR="v$(echo "$TAG" | cut -d. -f1 | tr -d v)"
          MINOR="$(echo "$TAG" | cut -d. -f1-2)"
          git tag -f "$MAJOR" "$TAG"
          git tag -f "$MINOR" "$TAG"
          git push origin "$MAJOR" "$MINOR" --force

  update-homebrew:
    if: false  # temporarily disabled
    needs: [validate-tag, release]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.validate-tag.outputs.version }}
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true

      - name: Compute SHA256
        id: sha256
        run: |
          for platform in darwin_amd64 darwin_arm64 linux_amd64 linux_arm64; do
            file="artifacts/velocirepo_${VERSION}_${platform}.tar.gz"
            if [ ! -f "$file" ]; then
              echo "ERROR: Missing artifact: $file"
              ls -la artifacts/
              exit 1
            fi
          done

          darwin_amd64=$(sha256sum "artifacts/velocirepo_${VERSION}_darwin_amd64.tar.gz" | cut -d' ' -f1)
          darwin_arm64=$(sha256sum "artifacts/velocirepo_${VERSION}_darwin_arm64.tar.gz" | cut -d' ' -f1)
          linux_amd64=$(sha256sum "artifacts/velocirepo_${VERSION}_linux_amd64.tar.gz" | cut -d' ' -f1)
          linux_arm64=$(sha256sum "artifacts/velocirepo_${VERSION}_linux_arm64.tar.gz" | cut -d' ' -f1)

          {
            echo "darwin_amd64=$darwin_amd64"
            echo "darwin_arm64=$darwin_arm64"
            echo "linux_amd64=$linux_amd64"
            echo "linux_arm64=$linux_arm64"
          } >> "$GITHUB_OUTPUT"

      - name: Checkout tap repository
        uses: actions/checkout@v5
        with:
          repository: posit-dev/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Update formula
        env:
          DARWIN_AMD64_SHA: ${{ steps.sha256.outputs.darwin_amd64 }}
          DARWIN_ARM64_SHA: ${{ steps.sha256.outputs.darwin_arm64 }}
          LINUX_AMD64_SHA: ${{ steps.sha256.outputs.linux_amd64 }}
          LINUX_ARM64_SHA: ${{ steps.sha256.outputs.linux_arm64 }}
        run: |
          cat > homebrew-tap/Formula/velocirepo.rb << 'FORMULA'
          class Velocirepo < Formula
            desc "Track your open-source project's pulse across package registries, GitHub, and the web"
            homepage "https://github.com/posit-dev/velocirepo"
            version "VERSION_PLACEHOLDER"
            license "MIT"

            on_macos do
              if Hardware::CPU.intel?
                url "https://github.com/posit-dev/velocirepo/releases/download/v#{version}/velocirepo_#{version}_darwin_amd64.tar.gz"
                sha256 "DARWIN_AMD64_PLACEHOLDER"
              end
              if Hardware::CPU.arm?
                url "https://github.com/posit-dev/velocirepo/releases/download/v#{version}/velocirepo_#{version}_darwin_arm64.tar.gz"
                sha256 "DARWIN_ARM64_PLACEHOLDER"
              end
            end

            on_linux do
              if Hardware::CPU.intel?
                url "https://github.com/posit-dev/velocirepo/releases/download/v#{version}/velocirepo_#{version}_linux_amd64.tar.gz"
                sha256 "LINUX_AMD64_PLACEHOLDER"
              end
              if Hardware::CPU.arm?
                url "https://github.com/posit-dev/velocirepo/releases/download/v#{version}/velocirepo_#{version}_linux_arm64.tar.gz"
                sha256 "LINUX_ARM64_PLACEHOLDER"
              end
            end

            def install
              bin.install "velocirepo"
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/velocirepo version")
            end
          end
          FORMULA
          sed -i 's/VERSION_PLACEHOLDER/'"${VERSION}"'/' homebrew-tap/Formula/velocirepo.rb
          sed -i 's/DARWIN_AMD64_PLACEHOLDER/'"${DARWIN_AMD64_SHA}"'/' homebrew-tap/Formula/velocirepo.rb
          sed -i 's/DARWIN_ARM64_PLACEHOLDER/'"${DARWIN_ARM64_SHA}"'/' homebrew-tap/Formula/velocirepo.rb
          sed -i 's/LINUX_AMD64_PLACEHOLDER/'"${LINUX_AMD64_SHA}"'/' homebrew-tap/Formula/velocirepo.rb
          sed -i 's/LINUX_ARM64_PLACEHOLDER/'"${LINUX_ARM64_SHA}"'/' homebrew-tap/Formula/velocirepo.rb

      - name: Commit and push
        run: |
          cd homebrew-tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add Formula/velocirepo.rb
          if git diff --cached --quiet; then
            echo "No changes to formula, skipping commit"
            exit 0
          fi

          git commit -m "Update velocirepo to v${VERSION}"
          git push

  update-scoop:
    if: false  # temporarily disabled
    needs: [validate-tag, release]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.validate-tag.outputs.version }}
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true

      - name: Compute SHA256
        id: sha256
        run: |
          file="artifacts/velocirepo_${VERSION}_windows_amd64.zip"
          if [ ! -f "$file" ]; then
            echo "ERROR: Missing artifact: $file"
            ls -la artifacts/
            exit 1
          fi
          sha=$(sha256sum "$file" | cut -d' ' -f1)
          echo "windows_amd64=$sha" >> "$GITHUB_OUTPUT"

      - name: Checkout scoop bucket
        uses: actions/checkout@v5
        with:
          repository: posit-dev/scoop-bucket
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: scoop-bucket

      - name: Update manifest
        env:
          WINDOWS_AMD64_SHA: ${{ steps.sha256.outputs.windows_amd64 }}
        run: |
          cat > scoop-bucket/velocirepo.json << 'MANIFEST'
          {
            "version": "VERSION_PLACEHOLDER",
            "description": "Track your open-source project's pulse across package registries, GitHub, and the web",
            "homepage": "https://github.com/posit-dev/velocirepo",
            "license": "MIT",
            "architecture": {
              "64bit": {
                "url": "https://github.com/posit-dev/velocirepo/releases/download/vVERSION_PLACEHOLDER/velocirepo_VERSION_PLACEHOLDER_windows_amd64.zip",
                "hash": "WINDOWS_SHA_PLACEHOLDER"
              }
            },
            "bin": "velocirepo.exe",
            "checkver": "github",
            "autoupdate": {
              "architecture": {
                "64bit": {
                  "url": "https://github.com/posit-dev/velocirepo/releases/download/v$version/velocirepo_$version_windows_amd64.zip"
                }
              }
            }
          }
          MANIFEST
          sed -i "s/VERSION_PLACEHOLDER/${VERSION}/g" scoop-bucket/velocirepo.json
          sed -i "s/WINDOWS_SHA_PLACEHOLDER/${WINDOWS_AMD64_SHA}/" scoop-bucket/velocirepo.json

      - name: Commit and push
        run: |
          cd scoop-bucket
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add velocirepo.json
          if git diff --cached --quiet; then
            echo "No changes to manifest, skipping commit"
            exit 0
          fi

          git commit -m "Update velocirepo to v${VERSION}"
          git push

  publish-pypi:
    needs: [validate-tag, build-linux, build-darwin]
    runs-on: ubuntu-latest
    environment:
      name: release
    permissions:
      id-token: write
    env:
      VERSION: ${{ needs.validate-tag.outputs.version }}
    steps:
      - uses: actions/checkout@v5

      - uses: astral-sh/setup-uv@v6

      - uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true

      - name: Build wheels
        run: |
          cd python
          uv run --with hatchling build_wheels.py \
            --binary ../artifacts/velocirepo-linux-amd64 \
            --version "$VERSION" \
            --platform linux_amd64 \
            --output ../wheels

          uv run --with hatchling build_wheels.py \
            --binary ../artifacts/velocirepo-linux-arm64 \
            --version "$VERSION" \
            --platform linux_arm64 \
            --output ../wheels

          uv run --with hatchling build_wheels.py \
            --binary ../artifacts/velocirepo-darwin-amd64 \
            --version "$VERSION" \
            --platform darwin_amd64 \
            --output ../wheels

          uv run --with hatchling build_wheels.py \
            --binary ../artifacts/velocirepo-darwin-arm64 \
            --version "$VERSION" \
            --platform darwin_arm64 \
            --output ../wheels

          # Windows wheel temporarily disabled
          # uv run --with hatchling build_wheels.py \
          #   --binary ../artifacts/velocirepo-windows-amd64.exe \
          #   --version "$VERSION" \
          #   --platform windows_amd64 \
          #   --output ../wheels

      - name: List wheels
        run: ls -la wheels/

      - name: Publish to PyPI
        run: uv publish --trusted-publishing always wheels/*.whl

  smoke-test-pypi:
    needs: [validate-tag, publish-pypi]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.validate-tag.outputs.version }}
    steps:
      - uses: astral-sh/setup-uv@v6

      - name: Wait for PyPI availability
        run: |
          for i in $(seq 1 30); do
            if uv tool install "velocirepo==$VERSION" 2>/dev/null; then
              echo "Installed successfully on attempt $i"
              exit 0
            fi
            echo "Attempt $i: not yet available, waiting 10s..."
            sleep 10
          done
          echo "ERROR: Package not available on PyPI after 5 minutes"
          exit 1

      - name: Verify installation
        run: |
          velocirepo version | grep "$VERSION"
