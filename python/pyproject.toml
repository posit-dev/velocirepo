[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "velocirepo"
dynamic = ["version"]
description = "Track your open-source project's pulse across package registries, GitHub, and the web"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [{ name = "Jeroen Janssens", email = "jeroen@jeroenjanssens.com" }]
keywords = ["metrics", "github", "pypi", "analytics", "open-source"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Other",
    "Topic :: Software Development :: Libraries",
]

[project.urls]
Homepage = "https://github.com/posit-dev/velocirepo"
Repository = "https://github.com/posit-dev/velocirepo"
Issues = "https://github.com/posit-dev/velocirepo/issues"

[project.scripts]
velocirepo = "velocirepo.__main__:main"

[tool.hatch.version]
path = "src/velocirepo/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/velocirepo"]
